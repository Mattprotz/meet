export default function NumberOfEvents(){
    return (
        <div id="NOE">
            <input
                type="text"
                className="noe"
                placeholder="# of events (default 32)"
            />
        </div>

    )
}